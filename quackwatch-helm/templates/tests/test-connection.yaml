apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "QuackWatch-helm.fullname" . }}-test-connection"
  labels:
    {{- include "QuackWatch-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "QuackWatch-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
